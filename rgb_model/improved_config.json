{
  "model_config": {
    "num_classes": 7,
    "input_shape": [
      224,
      224,
      3
    ],
    "dropout_rate": 0.4,
    "l2_regularization": 0.0005
  },
  "training_config": {
    "samples_per_class": 1000,
    "batch_size": 64,
    "stage1_epochs": 10,
    "stage2_epochs": 15,
    "stage3_epochs": 10,
    "stage1_lr": 0.0005,
    "stage2_lr": 5e-05,
    "stage3_lr": 5e-06,
    "use_class_weights": true,
    "augmentation_strength": 1.5,
    "label_smoothing": 0.1,
    "warmup_epochs": 3
  }
}